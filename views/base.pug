doctype html 
html 
    head 
        title Vertex League
        meta(charset="utf-8")
        block stylesheets 
            link(rel="stylesheet" href="css/shared.css")
            link(rel="stylesheet" href="css/base.css")
        block scripts 
            script(type="text/javascript" src="js/base.js")
    body 
        .banner 
            .banner-logo
                img(src="img/vl-logo.png")
            .banner-title VERTEX LEAGUE
            .banner-nav 
                if(seasonData)
                    -var i = 0
                    while i < seasonData.totalSeasons
                        if (i == seasonData.selectedSeason.id)
                            a.nav-season.nav-season-active(href="/overview?season="+(i + 1) data-season-number=(i + 1))= "Season " + (i + 1)
                        else 
                            a.nav-season(href="/overview?season="+(i + 1) data-season-number=(i + 1))= "Season " + (i + 1)
                        - i++
            .banner-spacer
        .banner-dropdown-container
            .invis
            .invis
            .banner-dropdowns
                if(seasonData)
                    -var i = 0
                    while i < seasonData.totalSeasons 
                        .season-menu(id="dropdown-menu-season-" + (i + 1) data-season-number=(i + 1))
                            a.season-menu-item(href="/overview?season=" + (i + 1)) Overview
                            a.season-menu-item(href="/matches?season=" + (i + 1)) Matches
                            a.season-menu-item(href="/teams?season=" + (i + 1)) Teams 
                            a.season-menu-item(href="/map-pool?season=" + (i + 1)) Map Pool
                            a.season-menu-item(href="/rules?season=" + (i + 1)) Rules
                        - i++
            .invis

        .content 
            block content